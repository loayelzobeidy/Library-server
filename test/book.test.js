const mathOperations = require('../src/controllers/book.controller');

describe("book test", () => {
test("Addition of 2 numbers", () => {
// arrange and act
var result = 1+2

// assert
expect(result).toBe(3);
});
})